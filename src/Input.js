import React from 'react';

const Input = (props) => {
    
    const styles = {
        margin: 50,
    }

    return (
        <form style={styles}>
            <input type='text' />
        </form>
    )
}

export default Input;